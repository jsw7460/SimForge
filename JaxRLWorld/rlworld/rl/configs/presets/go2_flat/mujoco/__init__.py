"""MuJoCo (mjlab)-specific Go2 flat preset variants (e.g. ``gait_conditioned``).

The unified ``Go2FlatConfig`` lives in
``rlworld.rl.configs.presets.go2_flat.base``. This package is reserved
for variants whose body is genuinely MuJoCo-specific.
"""
